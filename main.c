#include <stdio.h>
#include <stdlib.h>

#include "songnode.h"

int convert_char_to_index(char c) {
  return c-'a';
}

// add songs
song_node * add_song(song_node table, artist, name) {
  
}

// search for a song
// search for an artist
// print out all the entries under a certain letter
// print out all the songs of a certain artist
// print out the entire library
// shuffle
// delete a song
// delete all the nodes

int main() {
  song_node *table[26];
  printf("testing makefile\n");
  return 0;
}
